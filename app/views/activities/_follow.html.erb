<%= form_for(current_user.user_ac_relas.build(activity_id: @activity.id),
             remote: true) do |f| %>
  <div><%= f.hidden_field :activity_id %></div>
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>