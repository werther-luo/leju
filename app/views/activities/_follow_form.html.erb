<% unless current_user?(@creator) %>
  <div id="follow_form">
  <% if current_user.following_for_activity?(@activity) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>